{
  "name": "@monitorme/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": ".electron/main.js",
  "scripts": {
    "build:main": "tsc -p .electron/tsconfig.json",
    "build": "npm run build:main && tsc -p tsconfig.json",
    "dev": "npm run build && electron .",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ws": "^8.18.0",
    "bonjour-service": "^1.2.1",
    "uuid": "^9.0.1",
    "@monitorme/common": "*"
  },
  "devDependencies": {
    "electron": "^30.0.0"
  }
}


