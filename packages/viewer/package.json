{
  "name": "@monitorme/viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build:renderer": "tsc -p tsconfig.json",
    "build:main": "tsc -p .electron/tsconfig.json",
    "build": "npm run build:renderer && npm run build:main",
    "dev": "npm run build && electron .",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "bonjour-service": "^1.2.1",
    "@monitorme/common": "*"
  },
  "devDependencies": {
    "electron": "^30.0.0"
  }
}


